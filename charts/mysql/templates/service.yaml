apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql.fullname" . }}-service
spec:
  type: NodePort
  selector:
    {{- include "mysql.labels" . | nindent 4 }}
  ports:
  - port: 3306
    targetPort: 3306
